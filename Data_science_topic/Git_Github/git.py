from cmath import log
from http.client import RESET_CONTENT


git diff -r HEAD data/northern.csv
git diff -r 
git add eastern

####
Ctrl-K: delete a line.
Ctrl-U: un-delete a line.
Ctrl-O: save the file ('O' stands for 'output').
Ctrl-X: exit the editor.

git commit -m "Program appears to have become self-aware."

If you accidentally mistype a commit message, you can change it using the --amend flag.

git commit --amend - m "new message"


git commit -m "Adding a reference."


git log
 space + q to exit
 
 git show (first caracter of hash code)
 
 to show the comist before the most Rrecent
 
 git diff -r HEAD~1
 
 git annotate file shows who made the last change to each line of a file and when.
 
 
 git diff HEAD..HEAD~1